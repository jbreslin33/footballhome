# Per-developer server stacks (same host as production).
# Columns: DEV_SLUG  FRONTEND_PORT  BACKEND_PORT  DB_PORT  HOST_PREFIX
#
# URL examples (after nginx + DNS):
#   https://jbreslin.dev.footballhome.org  →  127.0.0.1:3010
#   https://lbreslin.dev.footballhome.org →  127.0.0.1:3020
#
# Or hit the host port directly: http://<server-ip>:3010
#
# Add a row for each new person, pick free ports, then:
#   sudo make dev-init DEV=<slug>
#   sudo make dev-up   DEV=<slug>

jbreslin  3010  3011  5440  jbreslin
lbreslin  3020  3021  5442  lbreslin

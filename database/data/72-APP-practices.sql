-- ========================================
-- APP-CREATED PRACTICES
-- ========================================
-- Practice sessions created via the web application
-- This file is manually maintained and version controlled
--
-- GroupMe practices are imported separately via scripts
-- This is for practices created directly in the app
-- ========================================

-- Example (commented out):
-- INSERT INTO practices (id, team_id, event_id, location, notes)
-- VALUES (
--   'practice-uuid',
--   'team-uuid',
--   'event-uuid',  -- Must exist in events table
--   'Field 3',
--   'Bring cones'
-- ) ON CONFLICT (id) DO UPDATE SET
--   location = EXCLUDED.location,
--   notes = EXCLUDED.notes;

-- Add your app-created practices below:


# Football Home Environment Configuration
# Copy this file to .env and fill in your credentials

# =============================================================================
# ENVIRONMENT (Required)
# =============================================================================
# Values: dev | production
# - dev: Loads app data from ##u-*.sql files (development data)
# - production: Loads app data from ##p-*.sql files (production data)
ENVIRONMENT=dev

# =============================================================================
# TWILIO SMS CREDENTIALS (Optional - for SMS notifications)
# =============================================================================
# Get these from: https://console.twilio.com/
# 1. Sign up for Twilio account (free trial available)
# 2. Go to Console Dashboard
# 3. Find "Account SID" and "Auth Token"
# 4. Get a phone number: Phone Numbers > Manage > Buy a number
#
# Leave empty if not using SMS features yet (app will work without them)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_PHONE=

# =============================================================================
# GOOGLE SHEETS API (Optional - for CASA league scraping)
# =============================================================================
# Get these from: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google Sheets API
# 3. Create Service Account credentials
# 4. Download JSON key file
# 5. Share your Google Sheet with the service account email
#
# Leave empty if not scraping CASA data
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=

# =============================================================================
# GROUPME API (Optional - for GroupMe chat scraping)
# =============================================================================
# Get this from: https://dev.groupme.com/
# 1. Log in with your GroupMe account
# 2. Click "Access Token" at top right
# 3. Copy the token
#
# Leave empty if not scraping GroupMe data
GROUPME_ACCESS_TOKEN=
GROUPME_EMAIL=
GROUPME_PASSWORD=

# =============================================================================
# GOOGLE API (Optional - for Google services)
# =============================================================================
# Google Sheets/API authentication for CASA roster scraping
GOOGLE_SHEETS_EMAIL=
GOOGLE_SHEETS_PASSWORD=
GOOGLE_API_KEY=

# =============================================================================
# DATABASE (Optional - for local scraper access)
# =============================================================================
# Only needed if running scrapers that directly access the database
# Docker containers use their own internal database configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=footballhome
POSTGRES_USER=footballhome_user
POSTGRES_PASSWORD=footballhome_pass
